{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "node \"${CURSOR_PLUGIN_ROOT}/lib/session-hook.js\" start --platform cursor",
        "description": "Turn SecureAI on for Cursor if automatic protection is enabled"
      }
    ]
  }
}
