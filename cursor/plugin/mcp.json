{
  "mcpServers": {
    "secureai": {
      "command": "secureai",
      "args": ["status"],
      "env": {
        "SECUREAI_NOTE": "Replace with stdio MCP bridge that calls secureai-client; tools: secureai_status, secureai_protect, secureai_off"
      }
    }
  }
}
